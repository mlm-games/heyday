[package]
name = "app_ui"
version.workspace = true
edition.workspace = true

[dependencies]
repose-ui = "0.10.0"
repose-core = "0.10.0"
domain = { path = "../domain" }
crossbeam-channel.workspace = true
